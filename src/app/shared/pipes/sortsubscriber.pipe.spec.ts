import { SortsubscriberPipe } from './sortsubscriber.pipe';

describe('SortsubscriberPipe', () => {
  it('create an instance', () => {
    const pipe = new SortsubscriberPipe();
    expect(pipe).toBeTruthy();
  });
});
